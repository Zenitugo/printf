#include <cstdio>

/**
 * _printf writes the character c to stdout
 * @c: The character to print
 * Return: 1 if sccessful
 * On error, return -1 and ernno is set appropriately
 */

int _printf(const char * format, ...)
{
	return(write(1, &c, 1);
}

